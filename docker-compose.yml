version: "3.9"
services:
  superbot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
