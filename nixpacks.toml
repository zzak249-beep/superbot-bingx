# nixpacks.toml — solo usado si railway.toml no activa Dockerfile
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
# CORRECCIÓN: main.py está en la raíz, no en src/
cmd = "python main.py"
